import { lexemDefinition, namedSpecialValues } from "./config";
import { simpleType } from "./first";
import { objectToRegexMap } from "./regex_utils";

// console.log(lexemDefinition);
// console.log(objectToRegexMap(namedSpecialValues));
console.log(simpleType)
